VERSION 3.1.0

INCLUDE preamble.tex
CONFIG
INCLUDE computeRole.py

IMPORT
BEGIN

PARSE personalInfo

if academic:
  PARSE education

if master:
  PARSE employment
else:
  PARSE employment after_date="2021-01-01"

if academic:
    PARSE publications
    PARSE preprints
    PARSE funding
    PARSE service
    PARSE honors
    PARSE talks
    if master:
        PARSE projects
    PARSE skills
else:
    PARSE projects latest_k=5
    PARSE skills
    PARSE education
    PARSE honors show_details=False
    if ml:
        PARSE publications latest_k=5
